# Legacy Content Archive

These `.qmd` files are not part of the active blog build.

They were moved out of the project root to reduce clutter and make the active content set easier to maintain.

Active pages are controlled by `book.chapters` in `_quarto.yml`.
